// Pure, framework-free logic shared by web (and later mobile).
// No React, no fetch, no Next imports in here — that's what keeps it shareable.
export { formatServerTime } from "./format-server-time";
