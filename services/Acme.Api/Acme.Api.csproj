<Project Sdk="Microsoft.NET.Sdk.Web">

  <!--
    TODO(contract-pipeline) — yours to write. Goal: `dotnet build` emits ../../contracts/openapi.json.
    Hints, not answers:
      - package:  Microsoft.Extensions.ApiDescription.Server (add its version in Directory.Packages.props;
                  it's a build-time tool, so think about PrivateAssets / IncludeAssets)
      - props:    OpenApiGenerateDocumentsOnBuild, OpenApiDocumentsDirectory
      - gotcha:   the default file name is derived from the assembly name, not "openapi.json" —
                  look up OpenApiGenerateDocumentsOptions (the "fileName" option)
      - gotcha:   build-time generation boots your Program; anything that needs a DB/secret at startup
                  will break the build. Know how to detect you're in the generator.
  -->

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\libs\Acme.Application\Acme.Application.csproj" />
    <ProjectReference Include="..\..\libs\Acme.Infrastructure\Acme.Infrastructure.csproj" />
    <ProjectReference Include="..\..\libs\Acme.Contracts\Acme.Contracts.csproj" />
  </ItemGroup>

</Project>
